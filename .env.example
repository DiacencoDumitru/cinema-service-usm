# Backend (Docker Compose passes vars into backend service)
JWT_SECRET=replace-with-long-random-secret-min-32-chars

# Optional local frontend — leave empty to use Vite proxy / same-origin in Docker
# VITE_API_URL=
